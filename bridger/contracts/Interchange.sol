contract Interchange {
    bytes reserved;
    bytes mintable;
    bytes wrapped;

    function mine(
        bytes _reserved,
        bytes _mintable,
        bytes _wrapped,
        
        bytes proof
    ) public returns (bool) {
        
    }
}